import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
  // Brand
  static const Color primary = Color(0xFF7C5CFC);
  static const Color primaryDark = Color(0xFF6246E8);
  static const Color primaryLight = Color(0xFFEEE9FF);
  static const Color primarySoft = Color(0xFFF7F4FF);

  // ---------------------------------------------------------------------------
  // Secondary / Accent
  // ---------------------------------------------------------------------------
  static const Color accent = Color(0xFF9B8AFB);
  static const Color accentLight = Color(0xFFE8E2FF);

  // ---------------------------------------------------------------------------
  // Text
  // ---------------------------------------------------------------------------
  static const Color textPrimary = Color(0xFF202033);
  static const Color textSecondary = Color(0xFF666477);
  static const Color textTertiary = Color(0xFF77738A);
  static const Color textHint = Color(0xFFAAA6B8);

  // Backgrounds
  static const backgroundLight = Color(0xFFF8FAFC);
  static const backgroundDark = Color(0xFF111827);

  // Surfaces
  static const surfaceLight = Colors.white;
  static const surfaceDark = Color(0xFF1F2937);

  // ---------------------------------------------------------------------------
  // Background
  // ---------------------------------------------------------------------------
  static const Color background = Color(0xFFF7F4FF);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceSoft = Color(0xFFF3F0FA);

  static const textPrimaryDark = Colors.white;
  static const textSecondaryDark = Color(0xFF9CA3AF);

  // --------------------------------------------------------------------------- // Border // ---------------------------------------------------------------------------
  static const Color border = Color(0xFFE5E0F2);
  static const Color borderLight = Color(0xFFEEEAF7);
  // --------------------------------------------------------------------------- // Status // --------------------------------------------------------------------------- static const Color success = Color(0xFF22A06B); s
  static const Color warning = Color(0xFFE6A23C);
  static const Color error = Color(0xFFD64545);
  static const Color info = Color(0xFF4A90E2);


  // --------------------------------------------------------------------------- // Common // ---------------------------------------------------------------------------
   static const Color white = Colors.white;
   static const Color black = Colors.black;

  // Status
  static const success = Color(0xFF22C55E);

  // UI
  static const indicatorInactive = Color(0xFFCBD5E1);

}
